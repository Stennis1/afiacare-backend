NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://user:password@localhost:5432/afiacare?schema=public
JWT_SECRET=replace-with-a-long-random-string-at-least-32-chars
JWT_EXPIRES_IN=7d

# Comma-separated phone numbers that receive escalation SMS (HIGH/EMERGENCY).
# Same list is used as voice <Dial> bridgeToNumbers when the voice slice ships.
# For the demo: point this at a teammate's live phone in the room so the
# escalation actually rings something visible on stage.
ESCALATION_CHW_PHONES=
